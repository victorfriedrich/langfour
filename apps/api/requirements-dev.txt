# Development and test only. Not installed into the container image.
-r requirements.txt
pytest==8.3.2
# The pipeline tests classify real googleapiclient HttpErrors rather than
# strings that look like them, so the API client is needed to run them.
google-api-python-client==2.149.0
